#include "motor_control.h"

bool controlMotorSpeed(double currentRpm, double targetRpm) {
    // TODO: Implement PID and return true when speed reached

    return false;
}

void getLauncherState(Context& ctx) {
    // TODO: Read sensors. Fill in:
    //   ctx.launcherState.rpm   = <measured RPM>;
    //   ctx.launcherState.angle = <measured angle>;
    
    (void)ctx;  // suppress unused warning
}